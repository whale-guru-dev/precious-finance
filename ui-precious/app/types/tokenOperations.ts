export enum TokenOperations {
  convert = "convert",
  stake = "stake",
  unstake = "unstake",
  convertAndStake = "convertAndStake",
}

export enum ExtendedOperations {
  claim = "claim",
  liquidity = "liquidity",
}
